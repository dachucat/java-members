<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="course.linkflower.link.oneframework.members.dao.CarCompanyMapper">
    <insert id="add">
        insert into car_company(name, address, email, phone, icon)
        values(#{name},#{address},#{email},#{phone},#{icon})
    </insert>

    <update id="update">
        update car_company set name=#{name}, address=#{address}, email=#{email}, phone=#{phone}, icon=#{icon} where id=#{id}
    </update>
    
    <select id="getCarCompanyById" resultType="course.linkflower.link.oneframework.members.vo.carcompany.CarCompanyNoIdVo">
        select * from car_company where id=#{id}
    </select>

    <delete id="deleteById">
        delete from car_company where id=#{id}
    </delete>
</mapper>